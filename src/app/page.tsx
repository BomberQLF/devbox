export default function Home() {
  return (
    <main className="h-screen">
      <div className="flex flex-col justify-center items-center h-full text-6xl">
        <h1 className="text-[#adadad] font-extrabold uppercase">Devbox - Way To Sucess</h1>
      </div>
      {/* Create a component with a list array, this array will contain the different projects, then MAP this list in this component to project them */}
      {/* Init a default value to a projects card even if they don't exist yet, like => {name OR defaultValue} */}
    </main>
  );
}
